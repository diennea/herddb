language: java
sudo: false

cache:
  directories:
  - "$HOME/.m2"

jobs:
  include:
  - stage: test
    jdk: openjdk10
    script: mvn test -Pjenkins -Dmaven.test.redirectTestOutputToFile=true -Pherddb.file.requirefsync=false

branches:
  only:
  - master
